# ─────────────────────────────────────────────────────────────
# Qwen Image Edit — SERVER (GPU box) dependencies
# Install on the machine with the RTX 3090.
# ─────────────────────────────────────────────────────────────

# Core model stack
torch>=2.1.0
torchvision>=0.16.0
# Qwen-Image-Edit-2511 (default) needs QwenImageEditPlusPipeline — only in
# recent diffusers. If loading 2509/2511 errors, run: pip install -U diffusers
diffusers>=0.35.0
transformers>=4.45.0
accelerate>=0.30.0
safetensors>=0.4.0

# 4-bit / 8-bit quantization (required for QIE_PRECISION=4bit|8bit).
# On Windows use a recent wheel with native support:
#   pip install bitsandbytes>=0.43.0
bitsandbytes>=0.43.0

# GGUF K-quant loading (required for QIE_PRECISION=gguf — the recommended mode).
# Also needs a recent diffusers (2511 GGUF fix landed in diffusers PR #12894);
# if 'gguf' precision errors on load, install diffusers from git:
#   pip install -U git+https://github.com/huggingface/diffusers
gguf>=0.10.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # required by FastAPI for multipart/form-data uploads

# Image processing
Pillow>=9.0.0
numpy>=1.21.0
